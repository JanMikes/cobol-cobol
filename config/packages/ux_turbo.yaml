# Enable stateless CSRF protection for forms and logins/logouts
framework:
    csrf_protection:
        check_header: true

# Configure Turbo to work properly with HTTPS
# This ensures forms work correctly on production with SSL
when@prod:
    turbo:
        enabled: true
